# 图库管理器（Picture Manager）

一个自托管、单用户的图库系统，用来收集、打标签、整理从外部抓取或导入的图片。

## 术语表

**拥有者（Owner）**：
一个部署实例里唯一的那个管理员用户。每个实例只有一个拥有者，在"初始化"时产生，之后不能更换（除非重建实例）。
_避免叫_：admin、超级用户、root

**用户（User）**：
一个可以登录、持有"会话"的账号。当前部署模式下唯一的用户就是拥有者；保留这张表是为了不让数据模型写死成"永远只有一行"。
_避免叫_：账号、会员

**初始化（Setup）**：
一次性动作，用来创建拥有者。只有在拥有者还不存在时才能初始化；拥有者一旦建好，初始化就关闭，再来请求会被拒绝。
_避免叫_：注册、signup

**会话（Session）**：
一次登录状态，用一个存在 cookie 里的令牌表示。一个会话属于一个用户，固定时间后过期。
_避免叫_：token、login

## 标签

**标签（Tag）**：
贴在图片上的一个分类词。带一个"类别"（用于界面配色）和一个被废弃标记。同一张图可以贴多个标签。
_避免叫_：keyword、分类

**连带（Implication）**：
一条"前因自动带后果"的关系：给图打了前因标签，系统就自动补上后果标签；前因标签仍然保留、能被单独搜索。
连带在**写入时就算实**——打标签时（手动或抓取/导入）当场把前因的整条连带链（A 带 B、B 带 C）算出来，一次性写进 post_tags。所以 post_tags 里存的永远是"展开后的完整标签集"，搜索时直接查 post_tags 即可，不做读时递归。
两条不变量：(1) 连带关系是后加的，要对现有打了前因的图做一次回填，补写后果；(2) 删除连带关系是"黏"的——不主动从老图上撤掉已写入的后果标签（那些标签可能是直接打的或别的连带带来的，撤起来歧义）。
_避免叫_：别名、改名、继承

**废弃标签（Deprecated Tag）**：
被标记为"不建议再往新图上打"的标签。它不是改名、不做重定向，老图上已有的不动，只是输入或显示时给提醒。它和"连带"是两件独立的事，可以叠加。
_避免叫_：别名、被替换的标签

## 收藏

**收藏夹（Favorite）**：
一个有名字的图片集合（类似播放列表），里面的图带排序位置。拥有者可以建多个命名收藏夹。
_避免叫_：相册、分组

**默认收藏夹（Default Favorite）**：
系统为拥有者自动维护的一个特殊收藏夹，承载"星标这张图"的动作。点星标 = 加入默认收藏夹，再点 = 从默认收藏夹移除。一张图可以同时存在于默认收藏夹和其他命名收藏夹中。
_避免叫_：主收藏、星标列表

**收藏（动作，Favorite v.）**：
把一张图加入某个收藏夹。注意：本系统**不统计**一张图被收藏了多少次，Post 上没有"被收藏数"这个字段——是否收藏过只通过"是否在某个收藏夹里"来判断。
_避免叫_：点赞、星标（星标只是默认收藏夹的别名动作）

## 图片与去重

**图片（Post）**：
图库里的一张图。带来源信息（从哪个站点抓的/还是本地导入）、媒体文件路径、尺寸、md5 精确指纹、phash 感知指纹、评级、以及是否重复的标记。
_避免叫_：image、item

**原图（Original Post）**：
一组近似重复图里被选作"代表"的那张。重复图通过 `duplicate_of_id` 指向它的原图。
_避免叫_：主图、源图

**重复图（Duplicate Post）**：
与某张已存在图近似（同一张图被压缩/缩放过的版本）的图。导入时先按 md5 精确去重（字节完全一样直接跳过、不建记录）；phash 在导入后异步计算，命中近似则标记为重复图并填上 `duplicate_of_id` 指向原图。重复图在图库主视图默认隐藏，可在专门视图查看，仍可被加入收藏夹。误判的重复图可以"转差分"：清除重复标记、转入某个差分组（见"转差分"）。
_避免叫_：副本、相似图

**差分组（Variant Group）**：
手动维护的"同一作品的多个差分版本"集合。组内选一张**封面（Cover）**作为代表图（用于管理界面和详情页展示），封面可以更换。组员身份**不影响**图片在图库主视图的展示——组里每张图都正常出现。一张图最多属于一个差分组。差分组与"重复图"是两套互相独立的机制：重复图靠 phash 自动判定，差分组靠人手动维护。
_避免叫_：图集、合集、父子图

**转差分（Convert to Variant）**：
把一张"重复图"转为某个差分组的成员的动作：清除它的重复标记（`duplicate_of_id` 置空，从此不再是重复图、不再被主视图隐藏），同时把它加入目标差分组。这个动作不可自动逆转（转回重复图需要重新判定）。
_避免叫_：移组、归组

**评级（Rating）**：
一张图的内容分级，三档：`safe`（安全）/ `questionable`（擦边）/ `explicit`（露骨）。图库主视图和搜索默认只显示 safe，用户可显式切换到含擦边或全部。评级是搜索的一个过滤维度。
_避免叫_：分级、nsfw 标记

**搜索（Search）**：
按标签找图的方式。多个标签空格分隔 = **同时满足（AND）**，例 `cat dog` = 既打了 cat 又打了 dog 的图。不做取反、不做 OR、不做括号分组。因为 post_tags 存的就是连带展开后的完整标签集，搜索只需在 post_tags 上做 AND 匹配，无需读时递归。结果默认按入库时间倒序（最新在前）。评级作为隐式过滤叠加在搜索之上。
_避免叫_：查询、检索

## 抓取与来源

**来源（Source）**：
一张图是从哪来的：`source_site`（哪个站，如 danbooru）、`source_id`（该站上的帖子 id）、`source_url`。本地导入的图来源字段为空。一张图只记**最早抓到它的那一个来源**，不记多来源；之后从别的站再抓到同内容，走重复图流程，不覆盖原图的来源字段。
_避免叫_：出处

**抓取去重（Scrape Dedup）**：
反复抓取同一站点时避免重复建图的两段判重：(1) 抓取列表阶段按 `(source_site, source_id)` 查库，已存在的整条跳过（不下载，最省）；(2) 下载后算 md5 兜底，若已存在则按"重复图"流程处理（标记重复、指向原图，不另建主图）。`(source_site, source_id)` 对非空来源加唯一索引，数据库层兜底并发撞车。
_避免叫_：增量抓取

## 前端界面

**浏览页（Browse）**：
图库主页，无边框瀑布流展示图片。顶栏（毛玻璃、滚动渐隐）含搜索框、安全模式开关、登录/导入入口；左侧可滑出标签筛选抽屉。无限滚动加载下一页。
_避免叫_：首页、gallery

**详情页（Lightbox）**：
点开一张图的全屏沉浸查看界面（黑底、图片主体居中/右侧、左侧浮层信息面板）。**不是一个独立路由**，而是叠在浏览页（或收藏页等）之上的浮层，由 URL 参数 `?photoId=<id>` 驱动：会话内点图打开（push）、← → 翻页用 replace（避免后退地狱）、Esc/遮罩/后退键关闭（back）、刷新/直达读参数自动开。直达场景下禁用 ← → 翻页（无列表上下文），需 `GET /api/posts/{id}` 单图接口取数据。
_避免叫_：详情页路由、post page

**安全模式（Safe Mode）**：
按评级过滤内容的开关，默认开启。开启时后端给所有查询自动注入 `rating=safe`。状态挂在**会话**上（不挂用户）：每个新会话（登录）默认安全，会话内可临时关闭看全部，登出/过期/重新登录 → 新会话自动回安全。页面刷新（同一会话）不重置。
_避免叫_：nsfw 开关
